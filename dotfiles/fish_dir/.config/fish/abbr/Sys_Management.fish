abbr -a rk1     'sudo paccache -rk1'
abbr -a ruk0    'sudo paccache -ruk0'
abbr -a ug      'sudo grub-mkconfig -o /boot/grub/grub.cfg'
abbr -a ref     'sudo reflector -p https -a 12 --sort rate -c "jp,hk,sg" --save /etc/pacman.d/mirrorlist'
